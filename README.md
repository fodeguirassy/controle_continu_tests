# controle_continu_tests

This library exposes function that helps manipulate Strings

It uses phpunit as test framework

To run the tests : 
                   on your command line tool, cd until the project folder
                   enter "vendor/bin/phpunit tests"
